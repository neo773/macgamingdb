// Main games, standalone expansions, remakes, remasters, expanded games, and
// ports — everything a player would call "a game", excluding DLC and updates.
export const SEARCHABLE_IGDB_GAME_TYPES = [0, 4, 8, 9, 10, 11];
